# Ensemble Analysis Jobs

These small immutable job specifications coordinate readiness polling and
CPU-only ensemble launches. Runtime state, locks, per-case partials, and logs
live under the matching `/mnt` `analysis_jobs/<job-id>/` path.
