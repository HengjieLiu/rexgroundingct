# Seeded Scoped Caruana Runs

Each immutable run freezes the merged fresh roster, per-scope K=4 seeds,
candidate pool, search limit, tie-break, code hashes, and mirrored `/mnt`
runtime location. Large per-case partials and logs stay outside Git.
