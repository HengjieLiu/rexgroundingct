# Caruana Replacement Global Runs

Each immutable run records the complete basket path, every candidate trial,
frequency weights, timing, and diagnostic/formal status. Runtime partials are
kept under the matching `/mnt` path.
