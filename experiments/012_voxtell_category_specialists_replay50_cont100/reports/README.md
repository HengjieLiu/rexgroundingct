# Exp012 Final Reports

This directory contains finalized, repository-local Markdown snapshots for the
completed Exp012 run profiles. Runtime JSON, predictions, checkpoints, and the
live reports remain under `/mnt/shengdata1`.

- `r01_core_replay50.md`: final Run 1 report.
- `r02_2d_diffuse_replay_ratio.md`: final Run 2 report.
- `exp012_r01_r02_combined.md`: consolidated comparison, including one merged
  epoch-100 per-category table.

Regenerate the combined report with `scripts/rexgroundingct/merge_012_reports.py`.
