# Scripts

The first agent stage creates `run_stage.py` here. It is the only command entry
point and exposes the `lineage`, `cohorts`, `logits`, `atlas`, and `closeout`
phases declared in `experiment.yaml`.
