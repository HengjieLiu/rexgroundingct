# Artifacts

`manifest.json` records exact read-only inputs, cryptographically pinned
revision-2 executable provenance, required workspace outputs, and small
manifests that hash external runtime products. Verified inputs have exact size
and SHA-256 now; planned required outputs are filled and reverified only by
their declared producer and closeout stages.
