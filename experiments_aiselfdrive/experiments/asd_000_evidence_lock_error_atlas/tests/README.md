# Tests

Tests cover hashes and lineage drift, val80/val120 disjointness and leakage,
RAS/left-right geometry, conservative prompt parsing, tri-state labels,
component reconstruction, and deterministic clustered bootstrap output.
