# Results

Small JSON, CSV, and Markdown evidence produced by declared stages lives here.
`result.json` is an initialization sentinel until the executor records real
stage evidence. Heavy logits and predictions stay in the external runtime.
