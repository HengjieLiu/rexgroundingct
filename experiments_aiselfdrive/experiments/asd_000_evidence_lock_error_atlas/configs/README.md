# Configuration

`protocol.yaml` freezes revision-2 inputs, source hashes, cohort roles,
thresholds, metrics, anatomy certification, logit fallback, resource bounds,
and output paths. Val120 is explicitly historical internal replication, not
independent confirmation. Implementation code must not embed a conflicting
scientific contract.
