# Tests

Tests cover malformed and ambiguous COT, split leakage, quartet balance,
deterministic matching, lexical shortcuts, case separation, frozen hashes,
single/double-swap margins, zero identity, and val120 access denial.
