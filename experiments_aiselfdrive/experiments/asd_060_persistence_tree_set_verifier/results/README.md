# ASD-060 results

Closeout first reports baseline, candidate recall, and exact oracle. Learned
results are reported only when both prerequisite gates pass. The final result
must distinguish `no_go` for insufficient candidate headroom from
`inconclusive` when headroom exists but safe tri-state labels are unavailable.
