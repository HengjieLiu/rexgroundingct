# ASD-060 required tests

Tests cover all sixteen thresholds, canonical edges and tie breaks, brute-force
oracle equivalence, ancestor exclusion, empty/single/multiple selection,
unknown zero gradients, ASD-040 branch outcomes, optional feature freezing,
patient leakage, sealed-cohort denial, deterministic hashes, atomic markers,
and full-state resume.
