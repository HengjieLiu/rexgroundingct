# ASD-060 configurations

`protocol.yaml` freezes thresholds, forest topology, exact oracle, feature
registry rule, set model, controls, branches, cohorts, and gates. Device
placement and full-state resume location are the only operational overrides.
