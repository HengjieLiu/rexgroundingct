# ASD-030 artifact ownership

`manifest.json` distinguishes read-only dependency evidence, small workspace
records, and heavy external runtime outputs. Checkpoints, logits, candidate
arrays, and replay caches never belong in Git. Expected conditional T4
artifacts remain planned unless portfolio promotion occurs.
