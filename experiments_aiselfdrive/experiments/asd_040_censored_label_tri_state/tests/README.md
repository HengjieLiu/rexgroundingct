# ASD-040 required tests

Tests must cover all four censorship rules, patient-separated folds, hidden
manifest redaction, evaluator mount isolation, tri-state precedence, nnPU risk
non-negativity, pseudo-positive calibration, text-only shortcut controls,
pre-T4 cohort denial, deterministic hashes, epoch-zero identity, and full-state
resume.
