# ASD-040 configurations

`protocol.yaml` freezes censorship rules, fold assignment, features, models,
loss policies, cohort access, and gates. Scientific fields cannot be overridden
at launch. Worker placement and full-state resume paths are operational
overrides and must be recorded.
