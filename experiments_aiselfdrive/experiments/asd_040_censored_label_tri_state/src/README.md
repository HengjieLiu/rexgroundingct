# ASD-040 source contract

Local modules own entity censorship, patient hashing, manifest redaction,
candidate persistence, feature extraction, cross-fitting, nnPU risk,
calibration, leakage scanning, and paired metrics. Existing repository modules
may be imported read-only.

The hidden evaluator interface accepts only trial IDs and predictions; it
never returns per-trial hidden geometry to training code.
