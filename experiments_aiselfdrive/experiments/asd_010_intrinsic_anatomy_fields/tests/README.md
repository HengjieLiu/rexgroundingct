# Tests

Tests cover physical affine mapping, field quantization, prompt fallback,
zero-scale bitwise identity, swap/invert/permutation controls, val120 leakage,
matched schedules, resume state, and gate calculations.
