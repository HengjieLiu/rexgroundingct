# Tests

Tests cover CT-RATE path resolution, split exclusion, deterministic shards,
completion-marker integrity, geometry/orientation failures, retry classes, and
budget enforcement without launching Docker.
