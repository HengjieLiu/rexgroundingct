# ASD-050 configurations

`protocol.yaml` freezes eligibility, exclusions, prompt swaps, physical
reflection, quartet loss, controls, schedules, cohorts, and gates. Scientific
fields cannot be overridden. Device assignment and full-state resume location
are the only operational overrides.
