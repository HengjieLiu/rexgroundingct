# ASD-050 required tests

Tests cover double-swap byte identity, double-flip voxel/affine identity,
left/right direction on synthetic phantoms, all exclusions, zero counterfactual
gradient outside known masks, factual label geometry, arm initialization
identity, sealed-cohort denial, atomic markers, and full-state resume.
