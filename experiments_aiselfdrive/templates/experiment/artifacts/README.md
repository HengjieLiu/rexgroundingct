# Template artifact manifest

`manifest.json` demonstrates artifact ownership, lifecycle, producer, hash, and
retention fields. A scaffolded experiment must replace all template tokens and
declare every durable or heavyweight output before becoming `ready`.
