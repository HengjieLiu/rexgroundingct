# Experiment template

Copy this template only through the validated scaffolding workflow. Replace
every template token before setting an experiment to `ready`; placeholders are
rejected by `experimentctl validate`.

The template deliberately predeclares all five claim categories and one
required artifact. Remove neither obligation: replace them with concrete,
gate-linked claims and artifact records before the audited `replan` transition.
