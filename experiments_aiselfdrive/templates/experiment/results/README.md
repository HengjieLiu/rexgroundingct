# Template result

`result.json` is a schema-valid shape template for structured stage evidence.
It is not completion evidence until every token is replaced and the producing
stage's acceptance checks and artifact hashes have been verified.
