# Shared source

Only code reused by at least two self-drive experiments belongs here. Every
module must have corresponding tests under `shared/tests` and must not mutate
existing repository experiment trees.
